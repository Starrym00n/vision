# 视觉端与 STM32 控制端通信约定

版本：2026-08-17  
适用工程：STM32F407VET6 三子棋控制器 + MaixCAM 视觉程序

## 1. 串口物理层

| 项目 | 约定 |
|---|---|
| 控制器串口 | USART1 |
| STM32 TX/RX | PA9 / PA10 |
| 波特率 | 115200 |
| 数据格式 | 8 数据位、无校验、1 停止位（8N1） |
| 硬件流控 | 无 |
| 电平 | 3.3 V TTL，双方必须共地 |
| 编码 | UTF-8 / ASCII 兼容文本 |
| 帧格式 | 一条 JSON 占一行 |
| 帧结束符 | `\r\n`（CRLF，十六进制 `0D 0A`） |

接线：视觉 TX 接 STM32 PA10；如需双向通信，视觉 RX 接 STM32 PA9；双方 GND 相连。

## 2. 坐标系与机械参数

视觉端必须直接发送龙门架机械坐标，单位为毫米，不发送像素坐标。

- 机械零点：STM32 上电时确认的 `(0, 0, 0)`；
- X/Y 有效范围：`0.0～150.0 mm`；
- Z 坐标由 STM32 固件管理，视觉端不发送 Z；
- X/Y 坐标中任何一个小于 0 或大于 150，都必须发送 `status=5`；
- 当前 X/Y 打字机电机：每转 3200 脉冲、移动 40 mm，即 `80 pulses/mm`；`Z` 轴沿用 8 mm/圈，即 `400 pulses/mm`；
- 视觉坐标标定必须和 STM32 的同一个机械零点一致。

格号约定：

```text
1 2 3
4 5 6
7 8 9
```

颜色与状态：

```text
0 = 空
1 = 黑棋
2 = 白棋
3 = 不确定（不允许作为可执行帧）
```

## 3. JSON 顶层格式

视觉端发送：

```json
{
  "type": "VISION_RESULT",
  "seq": 1,
  "status": 0,
  "board": {},
  "piece_count": 1,
  "pieces": []
}
```

所有以下顶层字段均为必填：

| 字段 | 类型 | 约定 |
|---|---|---|
| `type` | 字符串 | 固定为 `VISION_RESULT` |
| `seq` | 整数 | `0～65535`，每发送一帧递增，65535 后回到 0 |
| `status` | 整数 | 视觉状态码，只有 0 可执行 |
| `board` | 对象 | 棋盘状态与九个格子 |
| `piece_count` | 整数 | 必须等于 `pieces` 数组实际长度，最大 16 |
| `pieces` | 数组 | 棋盘内和待取区的棋子列表 |

未知的额外字段允许存在，但以上字段不能缺失。

## 4. status 状态码

| status | 名称 | 含义 | STM32 是否执行 |
|---:|---|---|---|
| 0 | OK | 棋盘、棋子和坐标均稳定有效 | 是 |
| 1 | NO_BOARD | 未识别到棋盘 | 否 |
| 2 | BOARD_UNSTABLE | 棋盘或坐标尚未稳定 | 否 |
| 3 | PIECE_UNSTABLE | 棋子颜色/状态不确定 | 否 |
| 4 | DUP_CELL | 多个棋子重复占用同一格 | 否 |
| 5 | OUT_OF_RANGE | 至少一个机械坐标越界 | 否 |

视觉端应在连续至少 3 次识别结果稳定后再发送 `status=0`。瞬时抖动期间可以不发送；如果发送，必须使用对应的非零状态码。

## 5. board 对象

正常帧必须包含完整九格：

```json
"board": {
  "valid": true,
  "cx_mm": 85.0,
  "cy_mm": 61.5,
  "yaw_deg": 0.0,
  "cells": [
    {"id":1,"cx_mm":52.0,"cy_mm":28.5,"state":0},
    {"id":2,"cx_mm":85.0,"cy_mm":28.5,"state":0},
    {"id":3,"cx_mm":118.0,"cy_mm":28.5,"state":0},
    {"id":4,"cx_mm":52.0,"cy_mm":61.5,"state":0},
    {"id":5,"cx_mm":85.0,"cy_mm":61.5,"state":0},
    {"id":6,"cx_mm":118.0,"cy_mm":61.5,"state":0},
    {"id":7,"cx_mm":52.0,"cy_mm":94.5,"state":0},
    {"id":8,"cx_mm":85.0,"cy_mm":94.5,"state":0},
    {"id":9,"cx_mm":118.0,"cy_mm":94.5,"state":0}
  ]
}
```

要求：

- `valid=true` 时必须恰好有 9 个格子；
- `id` 必须覆盖 1～9，不能重复；
- 每个格子必须同时包含 `id/cx_mm/cy_mm/state`；
- `cx_mm/cy_mm` 必须是有限数值并位于 0～150；
- `state` 只能是 0、1、2、3；
- `status=0` 时不能存在 `state=3`。

错误帧建议使用：

```json
"board":{"valid":false,"cx_mm":0.0,"cy_mm":0.0,"yaw_deg":0.0,"cells":[]}
```

## 6. pieces 数组

每个棋子必须包含六个字段：

```json
{
  "id": 0,
  "color": 1,
  "area": 2,
  "cell": 0,
  "cx_mm": 4.5,
  "cy_mm": 33.5
}
```

字段约定：

| 字段 | 约定 |
|---|---|
| `id` | 非负整数；同一帧内建议唯一 |
| `color` | 1 黑棋，2 白棋 |
| `area` | 1 棋盘内，2 棋盘外待取区 |
| `cell` | `area=1` 时为 1～9；`area=2` 时必须为 0 |
| `cx_mm/cy_mm` | 棋子中心的机械毫米坐标 |

`piece_count` 必须与 `pieces` 数组长度完全相同，且不得超过 16。

STM32执行机器落子时，会优先寻找：

```text
color = 目标颜色
area  = 2
cell  = 0
```

即棋盘外待取棋子。

## 7. seq 时序约定

- 每实际发送一帧，`seq` 加 1；
- 同一画面心跳帧也应使用新 `seq`；
- STM32完成一次动作后，会等待一个与动作前不同的 `seq`；
- 视觉端必须在机械动作结束、画面稳定后发送新帧；
- 不得长期重复发送相同 `seq`，否则 STM32会停留在“等待刷新”。

## 8. 题目1/演示模式1时序

### 第一步：动作前

棋盘为空，并至少有一颗棋盘外黑棋：

```json
{"type":"VISION_RESULT","seq":1,"status":0,"board":{"valid":true,"cx_mm":85.0,"cy_mm":61.5,"yaw_deg":0.0,"cells":[{"id":1,"cx_mm":52.0,"cy_mm":28.5,"state":0},{"id":2,"cx_mm":85.0,"cy_mm":28.5,"state":0},{"id":3,"cx_mm":118.0,"cy_mm":28.5,"state":0},{"id":4,"cx_mm":52.0,"cy_mm":61.5,"state":0},{"id":5,"cx_mm":85.0,"cy_mm":61.5,"state":0},{"id":6,"cx_mm":118.0,"cy_mm":61.5,"state":0},{"id":7,"cx_mm":52.0,"cy_mm":94.5,"state":0},{"id":8,"cx_mm":85.0,"cy_mm":94.5,"state":0},{"id":9,"cx_mm":118.0,"cy_mm":94.5,"state":0}]},"piece_count":1,"pieces":[{"id":0,"color":1,"area":2,"cell":0,"cx_mm":4.5,"cy_mm":33.5}]}
```

STM32从待取坐标吸取黑棋并放到所选择的目标格（默认第5格）。

### 第二步：动作后刷新

视觉确认黑棋已经在第5格后，发送新 `seq`：

```json
{"type":"VISION_RESULT","seq":2,"status":0,"board":{"valid":true,"cx_mm":85.0,"cy_mm":61.5,"yaw_deg":0.0,"cells":[{"id":1,"cx_mm":52.0,"cy_mm":28.5,"state":0},{"id":2,"cx_mm":85.0,"cy_mm":28.5,"state":0},{"id":3,"cx_mm":118.0,"cy_mm":28.5,"state":0},{"id":4,"cx_mm":52.0,"cy_mm":61.5,"state":0},{"id":5,"cx_mm":85.0,"cy_mm":61.5,"state":1},{"id":6,"cx_mm":118.0,"cy_mm":61.5,"state":0},{"id":7,"cx_mm":52.0,"cy_mm":94.5,"state":0},{"id":8,"cx_mm":85.0,"cy_mm":94.5,"state":0},{"id":9,"cx_mm":118.0,"cy_mm":94.5,"state":0}]},"piece_count":1,"pieces":[{"id":0,"color":1,"area":1,"cell":5,"cx_mm":85.0,"cy_mm":61.5}]}
```

## 9. 发送前自检

视觉端每次发送前必须确认：

1. JSON 是单行，末尾有 CRLF；
2. `type` 固定为 `VISION_RESULT`；
3. `seq` 在 0～65535 内且持续递增；
4. `piece_count == len(pieces)`；
5. 正常帧有完整且不重复的 1～9 号格；
6. 所有坐标均在 0～150 mm；
7. `status=0` 时 `board.valid=true`；
8. `status=0` 时没有未知棋子状态；
9. 棋盘外棋子使用 `area=2, cell=0`；
10. 动作后必须发送新序号的刷新帧。

## 10. STM32巡检模式说明

当前固件已启用四角巡检：

```text
(0,0) → (150,0) → (150,150) → (0,150) → (0,0) → 循环
```

每个点会执行一次 Z 轴下降/抬升检查。巡检模式用于确认 40 mm/转参数、运动方向和有效行程。在巡检界面按 `KEY_MODE/PC6` 可退出巡检并进入正常模式选择。视觉联调应在退出巡检、STM32显示“等待视觉”后进行。


